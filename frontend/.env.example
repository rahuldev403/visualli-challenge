# Backend base URL. Baked in at BUILD time, not runtime — changing it on your
# host requires a redeploy/rebuild, not just saving the variable.
#
# Either name works (both prefixes are whitelisted in vite.config.ts):
VITE_API_URL=http://localhost:3001
# API_URL=http://localhost:3001
